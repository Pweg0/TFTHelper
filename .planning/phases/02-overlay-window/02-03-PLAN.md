---
phase: 02-overlay-window
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/main/overlay/BoardStatePoller.ts
  - src/main/overlay/BoardStatePoller.test.ts
  - src/main/startup.ts
  - src/main/index.ts
  - src/main/ipc/handlers.ts
  - src/renderer/overlay/OverlayApp.tsx
  - src/renderer/overlay/components/PlayerPanel.tsx
  - src/renderer/overlay/components/ChampionIcon.tsx
autonomous: true
requirements:
  - OVER-01
  - DATA-04
must_haves:
  truths:
    - "BoardStatePoller polls fetchGameData every 1s and pushes parsed board state to overlay via IPC"
    - "GameWatcher onGameStart creates overlay window and starts BoardStatePoller"
    - "GameWatcher onGameEnd stops polling but keeps last data displayed in overlay"
    - "Overlay UI shows each alive player: name, HP, level, champion icons"
    - "Players are sorted by HP descending in the overlay"
    - "Eliminated players do not appear in the overlay"
    - "Local player shows gold alongside HP and level"
    - "Text floats without background panel, white with dark drop shadow"
    - "Panel is ~200px wide on the right side of the screen"
  artifacts:
    - path: "src/main/overlay/BoardStatePoller.ts"
      provides: "1s polling loop that pushes parsed board state to overlay window"
      exports: ["BoardStatePoller"]
    - path: "src/main/overlay/BoardStatePoller.test.ts"
      provides: "Unit tests for poller start/stop and IPC push"
      min_lines: 40
    - path: "src/renderer/overlay/components/PlayerPanel.tsx"
      provides: "Per-player row with name, HP, level, champion icons"
      min_lines: 20
    - path: "src/renderer/overlay/components/ChampionIcon.tsx"
      provides: "Small champion icon with star level indicator"
      min_lines: 10
  key_links:
    - from: "src/main/overlay/BoardStatePoller.ts"
      to: "src/main/game/LiveClientAPI.ts"
      via: "import fetchGameData"
      pattern: "fetchGameData"
    - from: "src/main/overlay/BoardStatePoller.ts"
      to: "src/main/overlay/BoardStateParser.ts"
      via: "import parseBoardState"
      pattern: "parseBoardState"
    - from: "src/main/overlay/BoardStatePoller.ts"
      to: "overlay BrowserWindow"
      via: "overlayWin.webContents.send('board-state-update', boardState)"
      pattern: "webContents\\.send.*board-state-update"
    - from: "src/main/startup.ts"
      to: "src/main/overlay/OverlayWindow.ts"
      via: "import createOverlayWindow"
      pattern: "createOverlayWindow"
    - from: "src/main/startup.ts"
      to: "src/main/overlay/BoardStatePoller.ts"
      via: "import BoardStatePoller"
      pattern: "BoardStatePoller"
    - from: "src/renderer/overlay/OverlayApp.tsx"
      to: "src/renderer/overlay/components/PlayerPanel.tsx"
      via: "renders PlayerPanel for each player"
      pattern: "PlayerPanel"
---

<objective>
Wire board state polling to the overlay window and build the overlay React UI components that display live player data.

Purpose: This plan connects the data layer (Plan 01) to the window layer (Plan 02) by adding the 1s polling loop, IPC push, and React components that render each player's HP, level, and champions in a floating right-side panel.

Output: Complete data flow from Live Client API to overlay UI — GameWatcher triggers overlay creation, BoardStatePoller pushes parsed data every second, overlay renders player rows sorted by HP.
</objective>

<execution_context>
@C:/Users/JoJo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JoJo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-overlay-window/02-RESEARCH.md
@.planning/phases/02-overlay-window/02-01-SUMMARY.md
@.planning/phases/02-overlay-window/02-02-SUMMARY.md

@src/main/startup.ts
@src/main/index.ts
@src/main/game/GameWatcher.ts
@src/main/game/LiveClientAPI.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BoardStatePoller with 1s polling and IPC push</name>
  <files>src/main/overlay/BoardStatePoller.ts, src/main/overlay/BoardStatePoller.test.ts</files>
  <behavior>
    - start(overlayWin) begins a 1s setInterval that calls fetchGameData, parses with parseBoardState, and sends result via overlayWin.webContents.send('board-state-update', boardState)
    - stop() clears the interval
    - Does not send if overlayWin.isDestroyed() returns true
    - Does not send if fetchGameData returns null (game not running)
    - Calls parseBoardState on successful fetch before sending
    - Multiple start() calls clear previous interval first (no duplicate timers)
  </behavior>
  <action>
    Create `src/main/overlay/BoardStatePoller.ts`:
    - Import `fetchGameData` from `../game/LiveClientAPI`
    - Import `parseBoardState` from `./BoardStateParser`
    - Import `BrowserWindow` from `electron`
    - Export class `BoardStatePoller` with:
      - Private `intervalId: ReturnType<typeof setInterval> | null = null`
      - `start(overlayWin: BrowserWindow): void` — clears existing interval, sets new 1000ms interval that:
        1. Awaits `fetchGameData()`
        2. If data is null or overlayWin.isDestroyed(), skip
        3. Calls `parseBoardState(data)` to get DisplayPlayer[]
        4. Calls `overlayWin.webContents.send('board-state-update', players)`
      - `stop(): void` — clears interval, sets to null

    Create `src/main/overlay/BoardStatePoller.test.ts`:
    - Mock `fetchGameData` from `../game/LiveClientAPI`
    - Mock `parseBoardState` from `./BoardStateParser`
    - Use vi.useFakeTimers for interval testing
    - Test: start creates interval, advancing 1000ms triggers fetch + parse + send
    - Test: stop clears interval
    - Test: null fetchGameData result does not call send
    - Test: destroyed window does not call send
    - Test: multiple start() calls only have one active interval
  </action>
  <verify>
    <automated>npx vitest run src/main/overlay/BoardStatePoller.test.ts</automated>
  </verify>
  <done>BoardStatePoller polls every 1s, parses board state, pushes to overlay via IPC, handles edge cases (null data, destroyed window, duplicate starts). All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire startup, overlay lifecycle, and build overlay React components</name>
  <files>src/main/startup.ts, src/main/index.ts, src/main/ipc/handlers.ts, src/renderer/overlay/OverlayApp.tsx, src/renderer/overlay/components/PlayerPanel.tsx, src/renderer/overlay/components/ChampionIcon.tsx</files>
  <action>
    **Main process wiring:**

    1. Modify `src/main/startup.ts` — update the GameWatcher callbacks to manage overlay lifecycle:
       - Import `createOverlayWindow` from `./overlay/OverlayWindow`
       - Import `BoardStatePoller` from `./overlay/BoardStatePoller`
       - After step 7 (watcher.start()), BEFORE watcher.start(), create the overlay window:
         ```typescript
         const overlayWin = createOverlayWindow();
         const poller = new BoardStatePoller();
         ```
       - Update GameWatcher onGameStart callback to:
         a. Send `game-started` to main window (existing behavior)
         b. Call `poller.start(overlayWin)` to begin 1s board state polling
       - Update GameWatcher onGameEnd callback to:
         a. Send `game-ended` to main window (existing behavior)
         b. Call `poller.stop()` — but do NOT hide the overlay or clear data (locked decision: data persists until new game starts)
       - On win.on('closed'), also call `poller.stop()` for cleanup
       - IMPORTANT: createOverlayWindow is called ONCE at startup (not per game) because attachByTitle can only be called once. The overlay shows/hides via attach/detach events from electron-overlay-window.

    2. Modify `src/main/index.ts` — add `electron-overlay-window` to externalize list if needed (may already be handled by electron.vite.config.ts from Plan 02). No other changes needed since startup.ts handles the wiring.

    3. No changes to `src/main/ipc/handlers.ts` — the `set-ignore-mouse-events` handler was registered in OverlayWindow.ts (Plan 02). Keep handlers.ts for main window IPC only.

    **Overlay React components:**

    4. Update `src/renderer/overlay/OverlayApp.tsx`:
       - Import `useState`, `useEffect` from React
       - Import `PlayerPanel` from `./components/PlayerPanel`
       - Import the DisplayPlayer type (define inline or import from a shared types file)
       - Define `DisplayPlayer` interface inline (matching the shape from BoardStateParser — do NOT import from main process):
         ```typescript
         interface DisplayPlayer {
           summonerName: string;
           hp: number;
           maxHp: number;
           level: number;
           champions: { name: string; starLevel: number; items: string[] }[];
           isLocalPlayer: boolean;
           gold?: number;
         }
         ```
       - Component state: `players: DisplayPlayer[]` initialized to `[]`
       - useEffect on mount:
         a. Call `window.overlayApi.toggleClickThrough(true)` to establish initial click-through
         b. Register `window.overlayApi.onBoardStateUpdate((data) => setPlayers(data as DisplayPlayer[]))`
       - Render:
         a. Outer div: position fixed, inset 0, background transparent, pointerEvents 'none'
         b. Panel div: position fixed, right 0, top 0, width 200px, height 100vh, display flex, flexDirection column, justifyContent center, gap 2px, padding 8px 4px, pointerEvents 'auto' (interactive zone)
         c. Panel div has onMouseEnter → `window.overlayApi.toggleClickThrough(false)` and onMouseLeave → `window.overlayApi.toggleClickThrough(true)`
         d. Map `players` array to `<PlayerPanel player={player} key={player.summonerName} />`
         e. If players is empty, show nothing (transparent, invisible)

    5. Create `src/renderer/overlay/components/PlayerPanel.tsx`:
       - Props: `{ player: DisplayPlayer }`
       - Render a horizontal row with floating text style (no background):
         a. Player name (12px, white, bold, drop shadow: `0px 1px 3px rgba(0,0,0,0.9), 0px 0px 6px rgba(0,0,0,0.7)`)
         b. HP display: `{hp}` in white (or gold for local player). If hp/maxHp data available, show as text
         c. Level: `Lv{level}` in slightly smaller text (10px)
         d. Gold (only for local player): gold-colored text showing `{gold}g`
         e. Row of ChampionIcon components for each champion
       - All text uses the floating style: white color, font-weight 600, text-shadow for legibility, no background panel
       - Font: "Segoe UI", Arial, sans-serif
       - Layout: compact, each player row ~24px height, name left-aligned, stats right-aligned

    6. Create `src/renderer/overlay/components/ChampionIcon.tsx`:
       - Props: `{ name: string; starLevel: number }`
       - Render a small square (22x22px) with:
         a. For now, show first 2 characters of champion name as text fallback (icon loading from cache is Phase 3 scope — keep it simple)
         b. Star level indicator: small dots or stars below the icon (1/2/3 stars)
         c. Drop shadow filter for legibility
         d. Border radius 3px
       - Use inline styles consistent with the floating text aesthetic

    **Visual style notes (locked decisions):**
    - No background panels or cards — text and icons float directly over the game
    - White text with dark drop shadow for contrast
    - Panel width ~200px on the right side
    - Eliminated players already filtered out by parseBoardState
    - Players sorted by HP descending (already done by parseBoardState)
  </action>
  <verify>
    <automated>npx electron-vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Startup creates overlay window once, GameWatcher triggers poller start/stop, overlay React components render player rows with floating text style. Full build succeeds. Full test suite passes: npx vitest run</done>
</task>

</tasks>

<verification>
npx vitest run && npx electron-vite build
</verification>

<success_criteria>
- BoardStatePoller polls at 1s, parses, pushes to overlay via IPC
- startup.ts creates overlay once at startup, starts/stops poller on game events
- Overlay React app receives board state updates and renders PlayerPanel per alive player
- Floating text style with drop shadow, no background panels
- Right-side panel ~200px wide
- Local player shows gold
- Build and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-overlay-window/02-03-SUMMARY.md`
</output>
