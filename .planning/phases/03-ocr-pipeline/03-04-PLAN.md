---
phase: 03-ocr-pipeline
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/renderer/overlay/OverlayApp.tsx
  - src/renderer/overlay/components/BoardDisplay.tsx
  - src/renderer/overlay/components/ShopHighlight.tsx
  - src/renderer/overlay/components/OCRStatusDot.tsx
  - src/main/startup.ts
autonomous: false
requirements:
  - DATA-04
  - DATA-05
  - OVER-03

must_haves:
  truths:
    - "Overlay shows a horizontal row of small champion icons for the local player's board"
    - "Each board champion icon shows a star level indicator (1/2/3 stars)"
    - "Golden borders are drawn over the 5 shop slots when a shop champion is already owned"
    - "Golden borders only appear when shop is visible (hidden during combat)"
    - "Small status dot in overlay: green when OCR active, red when offline/stale"
    - "Unrecognized champions show '?' placeholder"
  artifacts:
    - path: "src/renderer/overlay/components/BoardDisplay.tsx"
      provides: "Horizontal row of champion icons with star levels"
    - path: "src/renderer/overlay/components/ShopHighlight.tsx"
      provides: "Golden border overlay on shop slot coordinates"
    - path: "src/renderer/overlay/components/OCRStatusDot.tsx"
      provides: "Green/red status indicator dot"
    - path: "src/renderer/overlay/OverlayApp.tsx"
      provides: "Updated overlay integrating all new components"
  key_links:
    - from: "src/renderer/overlay/OverlayApp.tsx"
      to: "src/renderer/overlay/components/BoardDisplay.tsx"
      via: "renders board data from OverlayState"
      pattern: "<BoardDisplay"
    - from: "src/renderer/overlay/OverlayApp.tsx"
      to: "src/renderer/overlay/components/ShopHighlight.tsx"
      via: "renders shop borders when shopVisible and owned"
      pattern: "<ShopHighlight"
    - from: "src/renderer/overlay/OverlayApp.tsx"
      to: "src/renderer/overlay/components/OCRStatusDot.tsx"
      via: "renders status indicator from ocrStatus"
      pattern: "<OCRStatusDot"
---

<objective>
Build overlay UI components for board display, shop highlighting, and OCR status indicator. Wire OCRPipeline initialization into startup.ts. Final checkpoint for visual verification.

Purpose: OVER-03 (shop highlighting), DATA-04/DATA-05 visual feedback. This is what the user actually sees.
Output: Complete overlay UI with board icons, golden shop borders, status dot, and startup wiring.
</objective>

<execution_context>
@C:/Users/JoJo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JoJo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ocr-pipeline/03-RESEARCH.md
@.planning/phases/03-ocr-pipeline/03-01-SUMMARY.md
@.planning/phases/03-ocr-pipeline/03-02-SUMMARY.md
@.planning/phases/03-ocr-pipeline/03-03-SUMMARY.md

<interfaces>
<!-- OverlayState with OCR fields (from Plan 01+03) -->
From src/main/game/types.ts:
```typescript
export interface OverlayState {
  gold: number; level: number; gameTime: number; playerNames: string[]; localPlayerName: string;
  board: OCRChampion[]; bench: OCRChampion[]; shop: ShopSlot[]; shopVisible: boolean; ocrStatus: OCRStatus;
}
export interface OCRChampion { apiName: string | null; starLevel: 1 | 2 | 3; itemApiNames: string[] }
export interface ShopSlot { apiName: string | null; cost: number | null; owned: boolean }
export type OCRStatus = 'active' | 'stale' | 'offline';
```

From src/main/ocr/OCRCoordinates.ts:
```typescript
export const SHOP_SLOT_CENTERS: readonly { x: number; y: number }[];
```

From src/renderer/overlay/OverlayApp.tsx (existing):
```typescript
// Current overlay: right-aligned panel with local player info (name, level, time) and player list
// Uses pointerEvents: none on outer div, auto on panel with mouse enter/leave click-through toggle
// TEXT_SHADOW constant for text readability
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Board display, shop highlighting, and status dot components</name>
  <files>src/renderer/overlay/components/BoardDisplay.tsx, src/renderer/overlay/components/ShopHighlight.tsx, src/renderer/overlay/components/OCRStatusDot.tsx</files>
  <action>
1. Create `src/renderer/overlay/components/BoardDisplay.tsx`:
   - Props: `{ board: OCRChampion[], bench: OCRChampion[] }`
   - Renders a horizontal row of small champion squares (28x28px each, gap 2px)
   - Each square: dark background (#1a1a2e), rounded corners
   - Shows first 2 chars of apiName (or "?" if null) in white text with TEXT_SHADOW
   - Star level: render 1/2/3 gold dots below the icon (tiny circles, 4px, #ffd700)
   - Small item count badge in top-right corner if itemApiNames.length > 0
   - Combine board + bench into one row, separated by a thin vertical divider
   - Position: use Claude's discretion — suggest below the existing panel or along the bottom-left to avoid HUD overlap

2. Create `src/renderer/overlay/components/ShopHighlight.tsx`:
   - Props: `{ shop: ShopSlot[], shopVisible: boolean }`
   - Only renders when shopVisible is true
   - For each of 5 shop slots where `owned === true`:
     - Render a golden semi-transparent border (border: 3px solid rgba(255, 215, 0, 0.7)) at the absolute screen position
     - Position: absolute, using SHOP_SLOT_CENTERS coordinates (these are 1920x1080; the overlay window matches the game window size via electron-overlay-window, so coordinates map directly)
     - Each highlight box: approximately 120x120px centered on the shop slot center
   - pointerEvents: 'none' on all highlight elements (must not intercept game clicks)
   - Per user decision: simple yes/no golden border, no copy count

3. Create `src/renderer/overlay/components/OCRStatusDot.tsx`:
   - Props: `{ status: OCRStatus }`
   - Renders a 10px circle
   - green (#00ff00) when 'active', orange (#ff8c00) when 'stale', red (#ff0000) when 'offline'
   - Positioned in the overlay panel area (top-right, near existing info)
   - Subtle pulsing animation only on 'active' (CSS keyframes, opacity 0.7-1.0)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Three overlay components created: BoardDisplay shows champion row with stars, ShopHighlight draws golden borders on owned shop slots, OCRStatusDot shows green/red indicator.</done>
</task>

<task type="auto">
  <name>Task 2: Wire components into OverlayApp and OCRPipeline into startup</name>
  <files>src/renderer/overlay/OverlayApp.tsx, src/main/startup.ts</files>
  <action>
1. Update `src/renderer/overlay/OverlayApp.tsx`:
   - Update the local OverlayState interface to include OCR fields (board, bench, shop, shopVisible, ocrStatus)
   - Import BoardDisplay, ShopHighlight, OCRStatusDot from ./components/
   - Add OCRStatusDot next to the player name or in the panel header
   - Add BoardDisplay below the existing player info panel (or at a sensible overlay position)
   - Add ShopHighlight as a sibling to the main container (absolute positioned, full screen, pointerEvents none)
   - ShopHighlight renders outside the interactive panel (no mouse interference)
   - Handle missing OCR fields gracefully: default board=[], bench=[], shop=[], shopVisible=false, ocrStatus='offline' when state doesn't include them (backward compat during development)

2. Update `src/main/startup.ts`:
   - Import OCRPipeline
   - After static data is loaded (champions available), create OCRPipeline instance
   - Call `ocrPipeline.initialize(champions)` during startup sequence (after data download, before game watcher starts)
   - Pass ocrPipeline to BoardStatePoller (update constructor or add setter)
   - Add startup status step: 'ocr-init' between data loading and game watcher start
   - On app quit: call `ocrPipeline.terminate()` to clean up Tesseract worker
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json && npx vitest run</automated>
  </verify>
  <done>OverlayApp renders board icons, shop golden borders (when visible + owned), and OCR status dot. OCRPipeline initialized at startup with champion data. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of OCR overlay in live TFT game</name>
  <action>
  Human verifies the complete OCR pipeline visually in a live TFT game.

  What was built: Complete OCR pipeline — screen capture, shop reading, board reading, overlay UI with golden borders on owned shop champions, status dot, and champion icon display.

  How to verify:
    1. Run `npm run dev` to start the Electron app in development mode
    2. Launch a TFT game (or use Practice Tool)
    3. Wait for overlay to attach to the TFT window
    4. Verify: OCR status dot appears (green when game is running)
    5. During planning phase (shop visible):
       - Verify: if you own a champion that appears in shop, a golden border appears on that shop slot
       - Verify: board display shows small champion squares with abbreviated names
    6. During combat phase:
       - Verify: golden borders disappear (shop not visible)
       - Verify: board display still shows your champions
    7. If OCR is not reading correctly: check console for [OCRPipeline] logs showing what text is being recognized
  </action>
  <verify>Human visual confirmation</verify>
  <done>User confirms OCR reads shop/board correctly and golden borders appear on owned champions in shop. Type "approved" or describe issues for gap closure.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no type errors
- `npx vitest run` — full suite green
- Visual: overlay shows board champions, golden shop borders, status dot
</verification>

<success_criteria>
- Board display shows horizontal row of champion icons with star indicators
- Golden borders appear on shop slots containing champions the player owns (per user decision: drawn directly over game screen)
- Borders only visible when shop is on screen (hidden during combat per user decision)
- OCR status dot: green = active, red = offline (per user decision)
- "?" shown for unrecognized champions (per user decision)
- OCRPipeline initialized at startup, terminated on quit
- Human verifies visual overlay behavior in live TFT game
</success_criteria>

<output>
After completion, create `.planning/phases/03-ocr-pipeline/03-04-SUMMARY.md`
</output>
